
A Conversational layer demands one party keep track of its context in a client-server model of triggering Stateless services, there is no buildup of context. So, what we're going to do is create a shim or a proxy. In the same style as a caching reverse proxy, except what it caches is the context. So it's A stateful reverse proxy.
The State




---














